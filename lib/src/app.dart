import 'package:flutter/material.dart';
import 'package:flutter_app_form_login/src/resources/login_page.dart';
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new MaterialApp(
        home: LoginPage()
    );
  }
}